## MemeChat

### Install
Run the following commands to install the app:
```
git clone MemeChat
cd memechat
npm install
```
#### Run in production
Run the following command to run the app in production:
```
npm start

